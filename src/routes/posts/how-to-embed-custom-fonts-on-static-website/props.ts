import { getDateString } from '@/src/utils/dateutils'

const title = 'How to embed custom fonts on a static website with CSS'
const subtitle = 'The joy of setting a handwritten-like monospaced font on your terminal and website'
const publicationDate = getDateString('2024-07-04 05:22')

const props = {
    title,
    subtitle,
    authors: ['ravihlb'],
    publicationDate,
    URL: 'posts/how-to-embed-custom-fonts-on-static-website'
}

export default props
