import { getDateString } from '@/src/utils/dateutils'

const title = 'How to set which filetypes LSPs are run on in Neovim'
const publicationDate = getDateString('2024-06-25 05:22')

const propsPost1 = {
    title,
    authors: ['ravihlb'],
    publicationDate,
    URL: 'posts/neovim-filetypes-lsp'
}

export default propsPost1
